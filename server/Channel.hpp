#ifndef CHANNEL_HPP
# define CHANNEL_HPP

class Channel {
// OCCF
 public:
	Channel();
	Channel(const Channel& copy);
	Channel& operator = (const Channel& copy);
	~Channel();
// MEMBER VARIABLE
// MEMBER FUNCITON
};

#endif
